### CCNA

* layer 1 signal on wire
* layer 2 mac address
* layer 3 ip 

Switch is a layer 2 device. only uses mac addresses and populates cam table to know what devices are connected to what ports. Packets when on the switch are refered to as frames.
